data/raw/
data/grants.db
# data/grants.faiss and ml/model.pkl are intentionally NOT excluded here.
# They are built locally against Railway's PostgreSQL and committed to git,
# so Railway bakes them into the Docker image at build time.
__pycache__/
**/__pycache__/
.pytest_cache/
.env
*.pyc
*.pyo
.git/
.hf_cache/
